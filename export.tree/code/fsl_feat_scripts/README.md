## Description
Here you can find all .fsf scripts to run first, second and third level analyses in FSL (version I used is 6.0.0). Also scripts to generate the event onset files.
There is also a bash script to loop over all subjects for first and second level analyses. **Make sure to modify the absolute project path before running them.** 

